# TSMDB - Thiago Seva Movie Database

## Tech Stack
- React 19 + Vite (JavaScript, not TypeScript)
- shadcn/ui with Tailwind CSS v4, dark mode only
- Firebase: Auth (via Firebase UI), Firestore for data
  - Firebase UI (shadcn setup): https://github.com/firebase/firebaseui-web
- TMDB API for movie data
  - Developer docs: https://developer.themoviedb.org/docs/getting-started
  - v3 API reference: https://developer.themoviedb.org/reference/getting-started
- Dicebear API for user avatars (URL-based, no file storage needed)
  - Docs: https://www.dicebear.com/introduction/
  - React guide: https://www.dicebear.com/guides/use-the-library-with-react/

## Project Structure
- `src/pages/` - Route-level page components
- `src/components/ui/` - shadcn UI components (shared, generated by CLI)
- `src/components/` - Custom app components
- `src/firebase.js` - Firebase config and exports (app, auth, db, ui)
- `src/tmdb.js` - TMDB API helper; use `tmdbFetch(endpoint)` for all TMDB requests
- `src/lib/utils.js` - shadcn utility functions

## Routing & Auth
- Routes defined in `App.jsx`, wrapped with `FirebaseUIProvider` and `BrowserRouter`
- `/login` is the only public route (logged-out users)
- All other routes require authentication: `/`, `/browse`, `/film/:id`, `/account`, `/favorites`, `/results`
- Unauthenticated users should be redirected to `/login`
- `*` - 404 (Lost)

## Environment
- Env vars use `VITE_` prefix, stored in `.env` (not committed)
- `.env` must be shared directly between teammates, not via git

## Conventions
- Use JSX (not TSX)
- Import path alias: `@/` maps to `src/`
- Add shadcn components via: `npx shadcn@latest add <component>`
